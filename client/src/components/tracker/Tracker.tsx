import Sidebar from './Sidebar';
import Calendar from './Calendar';

const Tracker = () => {
	return (
		<div className='tracker'>
			<Sidebar />
			<Calendar />
		</div>
	);
};

export default Tracker;
